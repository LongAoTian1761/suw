# 给老师的说明

> 这份文档是写给任课老师的。你可以直接把它发过去，或者打印出来当面交给老师。

## 这是什么

一个**课后题解答收集网站**。同学打开网页，选好题号、写下自己的解答（支持数学公式排版）、
把手写照片或 PDF 拖进去，点一下提交就完成了 —— **不需要注册任何账号，也不需要登录**。
提交上来的作业会自动按习题归类，你随时可以批改、也可以把不合适的那条撤下来。

网站已经做好了，包含教材 **Schmitt-Grohé / Uribe / Woodford《International Macroeconomics》**
（2021 年 5 月草稿版）的 **15 章 161 道课后题**，每道题一个独立页面。

## 老师需要做什么

只有两件事，加起来大约 15 分钟，而且**完全免费**。

### 第一件：注册一个 Supabase 账号（约 10 分钟）

Supabase 是存放作业和附件的地方，免费额度对一个班来说绰绰有余
（500 MB 数据库 + 1 GB 文件存储，按作业量够用很多年）。

1. 到 [supabase.com](https://supabase.com) 用学校邮箱注册，新建一个 Project。区域建议选 **Southeast Asia (Singapore)**，国内访问快。
2. 打开左侧 **SQL Editor → New query**，把项目里 `supabase/schema.sql` 的内容整份粘进去，点 **Run**。看到 `Success` 就好了。
3. 打开左侧 **Project Settings → API Keys**，复制 **Publishable key**（`sb_publishable_...`）。
4. 把项目地址（形如 `https://xxxx.supabase.co`）和这个 key 填进 `data/course.json` 里，然后跑一次 `node scripts/build.mjs`。

> 这个 key 是设计成公开的，写在网页里没有安全问题。
> 真正拦人的是第 2 步那段 SQL 建的权限规则 —— 同学能交作业，但改不了别人的、也看不到没有公开的内容。

### 第二件：放到公网上（约 5 分钟）

在 GitHub 建一个空仓库，把项目推上去，然后在仓库 **Settings → Pages** 里
把 Source 选成 `Deploy from a branch`，Branch 选 `main`、目录选 `/ (root)`。

一两分钟后访问 `https://<你的用户名>.github.io/<仓库名>/`，把网址发给同学就行了。

## 同学那边是什么体验

一句话就够：

> 作业答案传到 \<网址\>/submit.html，不用注册，姓名可以填「匿名」，手写照片直接拖进去。

交完之后，页面下方「我的提交」里能看到自己交过的作业，**写错了可以随时修改，也可以撤回**。
撤回之后网站上就看不到了，想恢复点一下就行。

## 老师怎么批改

### 方法一：网页界面（推荐）

在电脑上打开命令行，进到项目目录，先建一个 `.env.local` 文件：

```
SUPABASE_URL=https://你的项目地址.supabase.co
SUPABASE_SERVICE_ROLE_KEY=你的 Secret key
```

> Secret key 在 **Project Settings → API Keys → Secret keys** 里。
> 注意：这个 key 相当于管理员密码，**只能放在你自己的电脑上**，
> 不要提交到 GitHub，也不要写进网页。`.env.local` 已经被列为不提交文件。

然后运行：

```bash
node scripts/grade-ui.mjs
```

浏览器打开 http://127.0.0.1:4190 就是批改界面：左边是作业列表（待批改的排在前面），
右边能看到同学的原文、附件，下面可以填**等级/分数**和**批语**，点保存即可。
批语支持 Markdown 和公式，会公开显示在同学的答案下面。

### 方法二：在 Supabase 网页里改

登录 Supabase，左侧 **Table Editor → submissions**，直接改 `grade` 和 `feedback` 两列。
不需要装任何东西，手机上也能用，只是看得没那么舒服。

## 几个常见问题

**要花钱吗？** 不用。Supabase 免费版和 GitHub Pages 都免费。

**同学填的学号会不会被别人看到？**
不会。公开页面上只显示姓名（或昵称）和答案内容。
班级、学号只有你在批改界面和 Supabase 后台能看到。

**如果分类错了怎么办？** 在批改界面把状态改成「撤下」就可以了，记录仍然保留。

**能不能改成「先审后放」？**
可以。现在是提交后立刻展示。想先审一遍：
把 `data/course.json` 里的 `autoApprove` 改成 `false`，
再把 `supabase/schema.sql` 里插入策略那行的 `'verified'` 改成 `'pending'`，
在 SQL Editor 里重跑那段，然后重新构建一次。

**题面是从教材 PDF 自动提取的，会不会有错？**
会有。分式、上下标这类复杂公式可能串位，每道题下面都标了「请以教材原文为准」。
如果你希望完全不显示题面（只保留题号和提交入口），
把 `data/course.json` 里的 `showProblemText` 改成 `false` 即可。

**数据归谁？** 归这个 Supabase 项目和 GitHub 仓库的所有者，也就是你。
建议一开始就用你自己的账号建，这样学生的作业数据从一开始就在你手上。
